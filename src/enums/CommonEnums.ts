export enum WEB_CALLBACK {
    POPUP = 'POPUP',
};

export enum WEB_DATA {
    BANNERS = 'BANNERS',
};

export enum TYPE_FILE {
    AUDIO = 'AUDIO',
    VIDEO = 'VIDEO',
    IMAGE = 'IMAGE',
};

