const localPath: string = `localhost:3000`;
const localRedirect: string = `localhost:3000`;

export const domains = {
    imageVi: 'https://img.vietqr.io/',
    localPath,
    localRedirect,

}